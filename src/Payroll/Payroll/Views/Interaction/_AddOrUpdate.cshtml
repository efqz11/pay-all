@model Notification
@{ 
    var employees = (List<Employee>)ViewBag.EmployeeId;
    var announcements = (SelectList)ViewBag.AnnouncementId;
}
@*<form asp-action="AddOrUpdate" asp-controller="Interaction" data-ajax="true" data-ajax-method="POST"  data-ajax-update="#weekly-schedule" data-ajax-begin="showModal()" data-ajax-success="sendNotification('success', 'Interaction record was just saved');hideModal()" id="addUpdateConnectionForm" data-ajax-failure="handleModalPostFailure">
    @Html.AntiForgeryToken()
    <input asp-for="Id" type="hidden" value="@Model.Id" />
    <input asp-for="AnnouncementId" type="hidden" value="@Model.AnnouncementId" />
    <header>
        <h2 class="modal__title" id="modal-1-title">
            @if (Model != null || Model.Id > 0)
            {
                <a class="modal__btn modal__btn-back" data-ajax="true" data-ajax-update=".modal__container" data-ajax-begin="showModal()" asp-action="ViewAnnouncement" asp-controller="Home" asp-route-id="@TempData["AnnouncementId"]">
                    <i class="fa fa-arrow-left"></i>
                </a>
            }

            @(Model == null || Model.Id <= 0 ? "Create New Interaction" : "Change Interaction")
            <br />
            <small>@Model.TriggerSource</small>
        </h2>
        <button class="modal__close" aria-label="Close modal" data-micromodal-close></button>
    </header>
    <main class="modal__content" id="modal-1-content">
        <div class="form-group">
            <label asp-for="EmployeeId" class="control-label">Choose Employee</label>
            <select asp-for="EmployeeId" class="form-control">
                <option value="0">Choose Employee</option>
                @foreach (var dept in employees.GroupBy(x => x.Department.Name))
                {
                    <optgroup label="@dept.Key">
                        @foreach (var emp in dept.ToList())
                        {
                            <option value="@emp.Id">@emp.NameDisplay</option>
                        }
                    </optgroup>
                }
            </select>
        </div>

        <div class="row">
            <div class="form-group col">
                <label asp-for="SentDate" class="control-label"></label>
                <input type="text" asp-for="SentDate" class="form-control has-datepicker" value="@Model.SentDate" />
                <span class="text-danger" asp-validation-for="SentDate"></span>
            </div>

            <div class="form-group col">
                <label asp-for="RecievedDate" class="control-label"></label>
                <input type="text" asp-for="RecievedDate" class="form-control has-datepicker" value="@Model.RecievedDate" />
                <span class="text-danger" asp-validation-for="RecievedDate"></span>
            </div>
        </div>

        <hr />

        <div class="form-group">
            <label class="custom-control custom-checkbox">
                <input type="checkbox" asp-for="IsRecieved" value="false" class="custom-control-input" tabindex="5">
                <span class="custom-control-label" for="chbxTerms">
                    Mark item as received
                </span>
            </label>
        </div>

    </main>
    <footer class="modal__footer">
        <button type="submit" class="modal__btn modal__btn-primary">Save</button>
        <button class="modal__btn" data-micromodal-close aria-label="Close this dialog window">Close</button>
        @if (Model != null && Model.Id > 0)
        {
            <a class="modal__btn modal__btn-danger float-right" asp-action="Remove" asp-route-id="@Model.Id" data-ajax-method="POST" data-ajax="true" data-ajax-failure="handleModalPostFailure" data-ajax-success="sendNotification('success', 'Interaction record was just removed');hideModal();$('div.item-@Model.Id').hide()" data-ajax-confirm="Are you sure you want to remove this Interaction record">Remove</a>
        }
    </footer>
</form>*@


<script>
    
    $(".has-datepicker").flatpickr({
        dateFormat: "j-M-Y"
    });
    
    $(".has-timepicker").flatpickr({
        enableTime: true,
        noCalendar: true,
        dateFormat: "H:i",
        time_24hr: true
    });

    $("#EmployeeId").select2({
        placeholder: "Search for employees",
    });

    $('#EmployeeId').on('select2:select', function (e) { 
        console.log('select event');
    });

</script>