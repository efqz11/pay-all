<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <security>
      <requestFiltering>
        <!-- This will handle requests up to 700MB (CD700) -->
        <requestLimits maxAllowedContentLength="737280000"  maxUrl="10241" maxQueryString="20481" />
      </requestFiltering>
    </security>
  </system.webServer>
</configuration>